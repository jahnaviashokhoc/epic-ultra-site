name: Build & Deploy EPICUltra Site

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Site ready for deployment
        run: echo "HTML files committed directly — no build step needed."
